# Finance-Digital-Twin (FDT)

## 프로젝트 목적

KeyFin의 **개인 금융 디지털 트윈(PFDT, Personal Finance Digital Twin)** 을 구현하고, 이를 기반으로 **개인 금융 AI 에이전트**를 만드는 저장소.

KeyFin은 "소비 내역을 분석해 카테고리별 예산을 안내하고, 나갈 돈은 알아서 챙겨주는, 계획적인 소비 습관을 만들어주는 생활 금융 관리 서비스"다. 이 저장소는 그중 다음을 담당한다.

- **디지털 트윈 코어**: 현재 재무 상태 스냅샷 `State(t)`, 개인화 행동 모델 `Behavior`, 회계 전이 함수 `Transition` 으로 구성된 결정론적 시뮬레이션 엔진
- **예측·시뮬레이션**: 30일 잔액 궤적 예측, What-if 지출 분기 비교, 몬테카를로 결제 부족 리스크, 목표 역산(주간 지출 상한 도출)
- **진단·처방**: 동적 Safe-to-Spend, 7대 예산 봉투 간 재배분 제안, 소비 가속도·우려 결제 감지
- **AI 에이전트 레이어**: 가맹점명 자동 분류, 자연어 질의의 의도·파라미터 추출, 백엔드 산출 수치를 그대로 전달하는 페르소나 코칭, 정책·팁 RAG

### 설계 원칙 (반드시 지킬 것)

1. **환각 없는 금융 코어**: 잔액, 상태 전이, 시뮬레이션, 이상 탐지 등 모든 숫자 연산은 백엔드 규칙·수학 알고리즘이 전담한다. LLM이 숫자를 만들거나 바꾸지 않는다.
2. **AI는 어댑터**: LLM/SLM은 비정형 텍스트 분류, 자연어 파라미터 추출, 코칭 문장 생성에만 쓴다.
3. **불변 원장 / 가상 샌드박스 분리**: 실제 원장 `State(t)` 는 불변. What-if는 인메모리 포크에서만 연산하고 폐기한다.
4. **실제 이체·결제 실행 기능은 범위 밖**: 이 저장소는 분석·시뮬레이션·코칭에 집중한다.

### 참고 문서

- `docs/01_KeyFin_기획의도.md` : 서비스 기획 배경과 기능 개요
- `docs/02_KeyFin_요구사항명세.md` : 요구사항 명세 (FR/NFR, P0~P2 우선순위)
- `docs/FDT.md` : 디지털 트윈 기능 명세서 (FDT-INP / SIM / ANL / INT 기능 ID)

기능을 구현할 때는 위 문서의 기능 ID(예: `FDT-SIM-02`, `FR-BGT-01`)를 기준으로 범위를 잡고, 커밋 메시지·코드 주석에서도 해당 ID를 참조한다.

## Git 규칙

- **원격**: `https://github.com/hojunjeon/FDT.git`, 브랜치는 `main` 하나만 사용한다.
- **작업 단위 커밋·푸시**: 하나의 의미 있는 작업(기능 하나, 버그 수정 하나, 문서 하나)이 끝날 때마다 바로 커밋하고 `main` 에 푸시한다. 여러 작업을 한 커밋에 몰아넣지 않는다.
- **커밋 메시지는 한글로** 작성한다.
  - 형식: `<유형>: <무엇을 했는지 한 줄 요약>`
  - 유형 예시: `기능`, `수정`, `문서`, `리팩터`, `테스트`, `설정`
  - 예: `기능: 30일 잔액 궤적 예측 엔진 추가 (FDT-SIM-01)`, `문서: CLAUDE.md 커밋 규칙 추가`
  - 본문이 필요하면 한 줄 띄우고 한글로 변경 이유를 적는다.
- 커밋 후 `git push origin main` 까지 수행해야 작업 완료로 본다.
