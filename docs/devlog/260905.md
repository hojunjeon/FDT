# 2026-09-05 개선 작업 기록

## 자동 검증 기반 추가 (O07)

- 기준 커밋: `ee4028960ad62ba9e2ad220321c2df4d70d567a3`.
- `.github/workflows/ci.yml`: main push/PR/수동 실행, 읽기 전용 권한, 15분 작업 제한.
- Python 3.12와 Node.js 22 환경에서 잠금 의존성 설치, 배포 패키지 빌드, Python 및 JavaScript 회귀 테스트를 실행한다.
- 최초 실행 `33944136037`: 작업 생성 전 실패. job env의 runner 컨텍스트 참조를 제거했다.
- 수정 후 원격 CI `33944181364`: Python 테스트와 배포 빌드 성공.
- 동일 커밋의 소스 배포본 로컬 검증: `python -m pytest -q`, **178 passed** (Python 3.13.5).

## 기준선 및 설계 보완

- `docs/04_FDT_리뷰보완.md`에 오류 상태·입력 제한·세션 및 로그·평가 표본의 추가 계약과 미해결 설계 과제를 명시했다.
- README에 보완 계약을 연결하고 로그 경로를 실제 세션별 구조로 수정했다.

## 금융 코어 및 평가 경계 수정

- 목표 계획의 월말 정기 수입을 시뮬레이터 일정과 통일하고, 목표 계산 기간을 최대 365일로 제한했다.
- 반복 고정비의 월말 기준일을 보존하고 카드 식별자를 그룹 키에 포함했다.
- 재배분 제안이 실제 공급액을 초과하지 않도록 수정했다.
- 동일 정규화 거래 ID·동일 내용은 중복 제거하고, 충돌하는 내용은 오류로 처리한다.
- 표본 부족 캘리브레이션은 `insufficient_data`로 보고하며 합격하지 않는다.
- 회귀 테스트 10개 추가. 이 단계 로컬 검증: **188 passed**.
- 실제 금융망 취소 이력, 중간 결제 안전성, 확률 모형의 보정은 이번 수정 범위가 아니다.

## 에이전트 오류·입력·일반 대화 분리

- 도구 실행의 `ok / needs_input / unsupported / error` 상태와 설명 충실도를 분리했다. 도구 실패는 성공형 코칭으로 보내지 않는다.
- 필수 금액·날짜 누락을 LLM이 임의로 채우지 못하게 하고, 금액·기간의 상한 및 정수 입력을 검증한다.
- 현금 결제 수단을 보존하고, 명시한 날짜를 0~60일로 잘라 맞추지 않는다.
- 일반 대화는 도구를 실행하지 않는 `chat` 경로로 분리했다. 기존 명시적 금융 도구는 유지한다.
- 미구현 정책 도구는 활성 LLM 도구 목록에서 제외했다. 가용성 캐시는 30초 후 갱신하며 한 번의 모델 응답에서 최대 4개 도구만 처리한다.
- 실제 기준일을 라우터 프롬프트에 전달한다.
- 회귀 테스트 24개 추가. 이 단계 로컬 검증: **212 passed**.
- 숫자의 필드별 의미 검증, 구조화된 후속 작업 상태, 실제 Ollama 품질 평가는 후속 과제다.

## 웹·화면·패키징 보완

- 로그 API 기본 비활성화 및 로컬 연결 제한. 여러 세션과 구형/신형 로그를 레코드 시간순으로 병합한다.
- 세션 유휴 만료 30분, 생성 중 세션 포함 최대 64개. 종료 대기 중 전역 잠금을 점유하지 않으며 만료된 세션은 계산하지 않는다.
- `/api/live` 추가, `/api/health`의 반복 시뮬레이션 제거, 내부 예외 메시지 외부 노출 축소.
- 실패 도구의 성공형 시각화 제거, 웹 응답에 실행 상태 전달.
- 화면 버전·세션 ID·AbortController로 늦은 응답과 종료 후 재활성화를 차단하고, 중복 시작 및 취소된 시작의 서버 세션 정리를 처리한다.
- 결측값을 0원/0%로 표시하지 않고, 위험 점수와 확률 단위를 분리했다. 예측은 실제 기간 제목과 중앙값/P10~P90 SVG 및 표를 제공한다.
- wheel에 데모 시드를 포함하고 외부 데이터 경로를 `FDT_SEED_DIR`로 지정할 수 있게 했다.
- 웹 회귀 테스트 12개와 Node.js DOM/비동기 회귀 테스트 7개 추가.
- 최종 로컬 검증: **Python 224 passed**, **JavaScript 7 passed**, `node --check`, `git diff --check` 통과.
- 설치된 wheel 검증을 CI에 추가했다. 구현 커밋 작성 시 원격 결과는 UNVERIFIED였으며, 아래 원격 검증 항목에서 완료를 확인했다.
- 실제 브라우저 시각 점검, Windows 실행기, 실제 Ollama 추론, 실제 금융망 연동은 검증하지 않았다.

## 원격 검증 완료 및 실행 문서 정리

- 코드 검증 커밋: `3a81462c8ce59bdc0341ef85f0bc2620fb8a47fe`.
- GitHub Actions 실행 `33945558559`, job `101250869378`: **completed / success**.
- 잠금 의존성, Python 3.12.3: **224 passed in 5.53s**.
- Node.js 22.23.2: **7 passed**, 실패·건너뛰기 없음.
- `uv build`: 소스 배포본 및 wheel 생성 성공.
- editable 설치를 wheel로 교체하고 `python -I`로 소스 디렉터리 영향을 배제한 실행 검증 성공: 포함된 3개 프로필, 원장·State·Risk·Room 계산, 정적 HTML 확인.
- 원격 빌드 산출물을 다시 받아 로컬에서 검증한 코드·정적 파일·테스트·패키지 설정 58개와 바이트 단위 비교: **차이 0개**.
- README에 uv 설치/실행/테스트 절차, 환경변수, 로그 API 기본 비활성화, 세션 및 입력 제한, 남은 모델 설계 과제를 정리했다.
- 기준선 대비 Python 회귀 테스트 46개, JavaScript 회귀 테스트 7개 추가. 모든 설계 제안이나 실제 서비스 보안까지 완료했다는 의미는 아니다.
